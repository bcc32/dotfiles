(defvar bcc32-enable-explain-pause-at-startup nil
  "If non-nil, enable `explain-pause-mode' at startup.")

(defvar bcc32-enable-auto-revert-debugging-at-startup nil
  "If non-nil, enable some debugging code for auto-revert at startup.")
