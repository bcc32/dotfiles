#!/bin/sh

set -eu

from=$1
shift
to=$1
shift

snapshot_path="/mnt/pool/@$from/snapshots"

snapsend \
    -delete-extraneous \
    -from "ssh://root@$from" -to "ssh://root@$to" \
    -from-path "$snapshot_path" -to-path "$snapshot_path" \
    "$@"
